<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.iitpfoodapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/foodicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/foodicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".currentStatus"></activity>
        <activity android:name=".FinalOrderList" />
        <activity android:name=".Quantity" />
        <activity android:name=".rotiRoll" />
        <activity android:name=".indianVegCurry" />
        <activity android:name=".cake" />
        <activity android:name=".snacks" />
        <activity android:name=".AryaCanteen" />
        <activity android:name=".Restaurants" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>